# 🎵 Prompt3 (Prompt-in-MP3)

**Prompt3** — это компактная Python-утилита для тех, кто занимается генерацией музыки и звука с помощью ИИ. Скрипт позволяет «намертво» вшить текстовый промпт прямо в метаданные MP3-файла, не повреждая при этом обложку, теги артиста или название альбома.

Теперь вы никогда не забудете, с помощью какого заклинания был создан тот самый идеальный трек.

## 🚀 Назначение
Скрипт записывает ваш промпт в специальное поле ID3-тегов (`TXXX:Prompt`). Это стандартный формат пользовательских текстовых данных, который поддерживается большинством продвинутых аудио-библиотек и редакторов метаданных.

**Основные преимущества:**
- ✅ Не портит встроенные изображения (Album Art).
- ✅ Сохраняет существующие теги (Artist, Title, Album).
- ✅ Предотвращает случайную перезапись данных (защита от дублей).

## 🛠 Установка

1. Клонируйте репозиторий.
2. Установите необходимые зависимости (библиотеку `mutagen`) одной командой:

```bash
pip install -r requirements.txt
```

## 📋 Использование

Запуск осуществляется через терминал (командную строку). Синтаксис:

```bash
python prompt3.py <путь_к_файлу> "<текст_промпта>" [опции]
```
Доступные опции:

| Опция          | Описание                                                      |
|----------------|---------------------------------------------------------------|
| <путь к файлу> | Путь к целевому MP3-файлу.                                    |
| prompt         | Текст промпта (обязательно заключайте в кавычки).             |
| --replace      | Разрешает перезапись, если тег Prompt уже существует в файле. |
| -h, --help     | Показать справку по всем командам.                            |

Примеры

1. Простая вставка промпта:

``` bash
python prompt3.py music.mp3 "Lo-fi hip hop, cozy fireplace, soft piano, 80bpm"
```   

2. Замена существующего промпта:

Если вы уже записывали промпт в этот файл ранее, скрипт выдаст предупреждение и завершится. Чтобы принудительно обновить информацию, используйте флаг `--replace`:
   
```bash
python prompt3.py music.mp3 "New improved prompt" --replace
```   

